# Dynamic configuration generated at build time
# This file provides version information extracted from the repository

params:
  testify:
    # Go version requirement (from go.mod)
    goVersion: '1.25.0'

    # Latest release tag (from git tags)
    latestRelease: 'v2.4.2'

    # Version message for the documentation set
    versionMessage: 'Documentation test for latest master'

    # Build timestamp
    buildTime: '2026-04-13T19:39:26Z'
